# Linux-only features

alias free='free -h'
alias psf='ps -faux'
alias svnkdiff='svn diff --git | kompare -o -'
alias ls='ls --color=auto'
# GNU dir is /bin/dir, so no alias.
alias pstree='pstree -Uul'
